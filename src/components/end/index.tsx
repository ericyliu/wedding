import { Section } from "../section";
import styles from "./index.module.css";

export const End = () => {
  return (
    <Section className={styles.container}>
      <h2 className={styles.header}>We hope to see you there!</h2>
    </Section>
  );
};
